<?php
declare(strict_types=1);

namespace App\{{ namespace }}\Presentation\API\Middleware;

use Closure;

class {{ name }}Middleware
{
    public function handle($request, Closure $next)
    {
        // Logic middleware
        return $next($request);
    }
}
