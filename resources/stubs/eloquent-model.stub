<?php
declare(strict_types=1);

namespace App\{{ namespace }}\Infrastructure\Persistence\Models;

use Illuminate\Database\Eloquent\Model;

class {{ name }} extends Model
{
    protected $table = '{{ table_name }}';

    protected $fillable = [
        {{ fillable }}
    ];

    public function __construct()
    {
        parent::__construct();
        $this->setConnection(config('{{ name }}.database.connections.' . config('{{ name }}.database.default')));
    }
}
